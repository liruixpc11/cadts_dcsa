2018-03-21
初步实现client主动从server中取文件并传输到本地，进度条实时显示问题；超时、出错后的临时文件删除问题。客户端、服务端交流的全过程都是采用‘4B 头部长度+头部字段’的格式。

未实现：超时时长的动态更新

问题：
1、client发送progress给server时会存在：
	大多数情况下客户端与服务端进度能保持同步，但多次试验后会存在一定的出错率（即当客户端最后一次发送100%的进度时，服务端能够接收100%的进度，但服务端控制台打印的结果却不是100，从而不会执行clear指令）。
2、关于client.py 中的 download(self, url, local_path, progress_listener=None)函数中的progress_listener参数不是很明白。
3、fetch_file函数中的 on_progress(c,url,progress,error=None)这个函数不是很明白。主要是on_progress 作为回调函数在本程序中的用法不是很清楚